using UnityEngine;

public class TestConflict : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // gei el que lo lea
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
